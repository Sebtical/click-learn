# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

_To fill in once the stack is chosen: install, run, test (including how to run a single test), lint/format._

## Architecture

_To fill in once real code exists — the big-picture structure that takes reading multiple files to understand, not a file-by-file inventory._

## Status

Scaffolded on 2026-09-24 via the `new-project` skill — no code yet. Once there's a real
codebase here, run `/init` so Claude Code fills in Commands and Architecture from what
actually exists, rather than hand-writing them now.
