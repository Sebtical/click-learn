# OS
Thumbs.db
.DS_Store

# Claude Code local overrides (personal, never shared)
.claude/settings.local.json

# Logs / temp
*.log
*.tmp

# Secrets (clés API, etc.) — ne jamais versionner
.env
.env.*
*secret*
*.key

# Secrets locaux pour `wrangler dev` (Cloudflare Worker)
.dev.vars

# Cache local de wrangler
.wrangler/
